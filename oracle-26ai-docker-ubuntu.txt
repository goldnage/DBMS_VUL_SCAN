
sudo docker pull container-registry.oracle.com/database/free:latest

sudo docker run --name oracle-26ai \
  -p 1521:1521 \
  -e ORACLE_PWD=YourSecurePassword1 \
  -v oracle_data:/opt/oracle/oradata \
  -d container-registry.oracle.com/database/free:latest
  
docker logs -f oracle-26ai

#DATABASE IS READY TO USE!

암호 리셋
docker exex oracle-26ai resetPassword 원하는암호

중단 
docker stop oracle-26ai
시작
docker start oracle-26ai

해당 컨테이너의 system 계정으로 접속

$ docker exec -it oracle /bin/bash
$bash-4.4$ sqlplus / as sysdba


sudo docker exec -it oracle-26ai sqlplus sys/YourSecurePassword1@//localhost:1521/FREE as sysdba



sql> SELECT name FROM v$database;
sql> SELECT name FROM v$pdbs;

# sys 계정접속
docker exec -it oracle23ai sqlplus sys as sysdba
# 컨테이너확인
show con_name;
# 컨테이너 리스트 확인후 변경
SELECT name, open_mode FROM v$pdbs;
ALTER SESSION SET CONTAINER = FREEPDB1;
# 사용자 생성
CREATE USER myuser IDENTIFIED BY "MyPassword123!";
GRANT CREATE SESSION TO myuser;
ALTER USER myuser QUOTA UNLIMITED ON USERS;
# 권한 추가
GRANT CREATE SESSION TO myuser;
GRANT CREATE TABLE TO myuser;
GRANT CREATE VIEW TO myuser;
GRANT CREATE SEQUENCE TO myuser;
GRANT CREATE PROCEDURE TO myuser;
GRANT CREATE TRIGGER TO myuser;

-- Source - https://stackoverflow.com/a/77392232
-- Posted by Norman Aberin, modified by community. See post 'Timeline' for change history
-- Retrieved 2026-04-26, License - CC BY-SA 4.0

create user myuser no authentication;
grant connect, resource to myuser;
grant unlimited tablespace to myuser; -- fine-tune this later

-- Source - https://stackoverflow.com/a/77392232
-- Posted by Norman Aberin, modified by community. See post 'Timeline' for change history
-- Retrieved 2026-04-26, License - CC BY-SA 4.0

-- Creating the Invoice Table in myuser schema
CREATE TABLE myuser.INVOICE (
   EXTERNAL_ID NUMBER(10) NOT NULL,
   DUEDATE DATE,
   PRIMARY KEY (EXTERNAL_ID)
);

-- Source - https://stackoverflow.com/a/77392232
-- Posted by Norman Aberin, modified by community. See post 'Timeline' for change history
-- Retrieved 2026-04-26, License - CC BY-SA 4.0

-- using sqlcl
connect sys@localhost:1521 as sysdba;

select table_name, owner from dba_tables where upper(TABLE_NAME) in ('INVOICE', 'LINE');

-- result
SQL> select table_name, owner from dba_tables where upper(TABLE_NAME) in ('INVOICE', 'LINE');

TABLE_NAME    OWNER     
_____________ _________ 
INVOICE       SYS    
LINE          SYS  


-- Source - https://stackoverflow.com/a/77392232
-- Posted by Norman Aberin, modified by community. See post 'Timeline' for change history
-- Retrieved 2026-04-26, License - CC BY-SA 4.0

-- using sqlcl
connect system@localhost:1521/FREEPDB1;

-- result
SQL> select table_name, owner from dba_tables where upper(TABLE_NAME) in ('INVOICE', 'LINE');

TABLE_NAME    OWNER     
_____________ _________ 
INVOICE       MYUSER    
LINE          MYUSER  




GRANT CREATE TYPE TO myuser;
GRANT CREATE SYNONYM TO myuser;
 
